#include <stdio.h>

int main() {
    // ASCII value of the character you want to print
    int asciiValue = 65;  // Change this to the ASCII value you need

    // Print the character corresponding to the ASCII value
    printf("The character for ASCII value %d is: %c\n", asciiValue, asciiValue);

    return 0;
}
